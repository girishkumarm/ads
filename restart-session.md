# Restart Instructions for Claude Code

Run this in a new Claude Code session:

```
git pull && cat CLAUDE.md
```

Then say:

"Re-read CLAUDE.md, schedule all 18 crons from the Scheduled Tasks table, start Telegram listener polling every 1 minute. Read memory files at /home/girish/.claude/projects/-home-girish-ads/memory/ for pending tasks. Key pending items: (1) Sunday Apr 6 reduce Google Ads budget Rs 5K to Rs 3K, (2) Run 20-agent deep review of all ads, (3) Google Ads API uses google-ads Python library (already installed). Test all APIs and confirm status."
